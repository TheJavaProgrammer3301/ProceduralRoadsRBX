import Graph from "./Graph";

const graph = new Graph();

const nodeIds = [graph.addNode(new Vector3(0, 0, 0)), graph.addNode(new Vector3(0, 0, 100)), graph.addNode(new Vector3(100, 0, 100)), graph.addNode(new Vector3(200, 0, 100))];